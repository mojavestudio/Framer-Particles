# AWS Configuration for Mojave Particles Plugin
# Copy this file to aws.env and fill in your values

# AWS S3 Bucket (will be created if it doesn't exist)
AWS_S3_BUCKET=mojave-particles-plugin

# AWS Region
AWS_REGION=us-east-1

# CloudFront Distribution ID (optional, for cache invalidation)
AWS_CLOUDFRONT_DISTRIBUTION_ID=

# Custom Domain (optional)
CUSTOM_DOMAIN=

# AWS Credentials (use AWS CLI, IAM roles, or environment variables)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_SESSION_TOKEN=your_session_token (if using temporary credentials) 